CCIE Best Practice（提前养成习惯）

在后续所有实验中，我们尽量使用以下形式，而不是直接使用 pip：

安装库：

python3 -m pip install requests

升级 pip：

python3 -m pip install --upgrade pip

查看已安装库：

python3 -m pip list

这样可以确保安装的包与当前 Python 解释器一致，这是企业环境和自动化项目中的推荐实践。